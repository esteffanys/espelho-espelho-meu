import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'br.edu.ifrn.espelhomeu',
  appName: 'espelho-meu',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
